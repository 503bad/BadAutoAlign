スタンドアローン前提で、以下のようなGUI・UI・UX実装はどうですか？\
意見をください。

* 複数トラックをドラッグアンドドロップで投入可能（頭出しができている前提で、音源全体を切り貼りはしない
* ガイドボーカル、オケ、補正対象ボーカルを登録するものとする。オケは必須ではないが、UIとしては3トラック構成を前提とする
* タイミングとピッチの自動補正ボタンをそれぞれ作り、個別に利用できるようにする。
* トラック上の検出した発声ごとにマークをつけ、相対的・視覚的につながりが分かるようにラインでつなぐ
* 補正対象のマーカーの位置を動かせるようにする。「再補正」のボタンを設置し、動かしたマーカーの位置を基準にタイミング補正が実施する機能を提供

前項が妥当で、正常に実装して一定の効果を得られたら以下も検討したい。

* かけ違いの可能性は捨てきれないので、マーカーの付替えができるよう、ラインをクリックすると解除。マーカー2つを選択（Shiftを押しながらクリックで複数選択イメージ）した状態で「接続」ボタンを押すと付け替えができるイメージ。その後「再補正」をおすともう一度補正し直す。

どうでしょうか。

